{
    "version": 2,
    "builds": [
        {
            "src": "sever/index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/server/(.*)",
            "dest": "/server"
        }
    ]
}
